name: flutter_workout_app_v2
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  hive: ^2.2.3 # for database persistence
  hive_flutter: ^1.1.0 # for flutter integration with hive

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  hive_generator: ^2.0.1 # for code generation (type adapters)
  build_runner: ^2.4.11 # for running code generation

flutter:

  uses-material-design: true